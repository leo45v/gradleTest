description = """This is a Gradle Test
  Project name: ${project.name}
"""

version = ""


task test {
  group = "Test group"
  description = "This is a test Task"
  
  doFirst {
    println "Doing Something"
  }
}

String getSonarQubeHost() {
  if(project.hasProperty("teamcity")) {
    return teamcity["teamcity.sonarqube.host"]
  }
  return "NO INTEGRATED"
}

String getSonarQubeToken() {
  if(project.hasProperty("token") {
    return project.property("token").toString()
  }
  return "NO TOKEN"
}

println "THIS IS TEAMCITY PARAM ${getSonarQubeHost()}"

println "THIS IS TEAMCITY PARAM ${getSonarQubeToken()}"

println teamcity

